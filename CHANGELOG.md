# Changelog

## 2025-10-17
- feat: add browser-based admin console (`admin.html`/`admin.js`) for managing `data/` JSON datasets with dynamic forms, import/export, and localStorage persistence.
- style: introduce `styles-admin.css` to align the CMS view with existing brand color tokens and responsive patterns.
- docs: update `README.md`, `docs/architecture.md`, and `data/schema.md` to reflect the new management workflow and schema-driven form generation.
