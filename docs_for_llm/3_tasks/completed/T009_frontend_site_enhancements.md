# T009 – 前端 `/site` 页面增强计划（功能拆解） _已完成_
> 更新时间：2025-11-21 ；聚焦功能，不含测试/报告。

## 目标
- 提升 `/site/history`、`/site/links`、`/site/papers` 的交互与筛选体验，保持与后端新参数对齐。

## 执行步骤（完成情况）
1) `/site/history`  
   - 增加显式“加载更多”按钮，保留 IntersectionObserver 自动加载。  
   - 支持年份区间与关键词筛选，UI 显示当前筛选与重置。  
   - 错误/重试 UI：请求失败时显示提示与“重试”按钮，记录筛选条件。
2) `/site/links`  
   - 增加分组折叠/展开，分类/分组/关键词筛选与“重置筛选”。  
   - 搜索/筛选无结果提示，支持扁平列表视图切换。  
   - 统计与加载更多逻辑适配新视图。
3) `/site/papers`  
   - 支持多标签筛选、年份区间、排序，选中状态可视化；摘要展开/收起；空态提示与重置。  
   - 保持 SSR 初始数据 + 客户端筛选。
4) 导航与共通体验  
   - `SiteHeader` 添加 `/site/links`、`/site/papers` 高亮状态；筛选/加载按钮样式统一。
5) 文档同步  
   - 进度已反映在 `docs_for_llm/3_tasks/T009_static_merge.md` 与 `dev_notes`。

> 测试/报告未执行（按当时要求暂缓）。后续如需透传后端筛选或调整样式，请基于此完成状态继续迭代。
